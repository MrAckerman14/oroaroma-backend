import brandLogo from 'src/assets/logo.png';

// Configuracion global de identidad.
// Cambia este valor para actualizar el nombre visible de toda la aplicacion.
export const appTheme = {
  name: 'Oro Aroma',
  // Archivo ubicado en src/assets. Cambialo al crear otro tema.
  logo: brandLogo,
  // Archivo ubicado en public. Cambialo al crear otro tema.
  favicon: '/icons/logo.png',
};
